# Repo + Personal Extension

Base team profile:

- `fleet/core/teams/product-delivery.yaml`

Personal extension:

- `~/.agent-org/projects/<project-id>/teams/product-delivery.yaml`

Use merge strategy from `fleet/docs/architecture.md`.
